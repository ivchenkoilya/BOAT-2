meta:
  environment: python
  toolchain:
    name: pip
    version: "3.12"

build:
  requirementsPath: requirements.txt
  useCache: true

run:
  scriptName: talent_entry_v153.py
  persistenceMount: /data
  containerPort: "80"
