meta:
  environment: python
  toolchain:
    name: pip
    version: "3.12"

build:
  requirementsPath: requirements.txt
  useCache: false

run:
  scriptName: talent_entry_v164.py
  persistenceMount: /data
  containerPort: "80"
