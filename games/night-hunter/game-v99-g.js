artGame(demo=false){state.demo=demo;initAudio();try{audio?.ac?.resume?.()}catch(_){}$('intro').classList.add('hidden');reset();state.running=true;banner(0);setCaption('Оружие заряжено. Зачисти пост охраны.',3);resize();raf=requestAnimationFrame(tick)}
async function prepare(){try{const d=await api('start',{game:'night-hunter'});state.sessionId=d.session_id;state.seed=Number(d.seed)||Date.now();state.time=Number(d.duration)||180;$('start').textContent='НАЧАТЬ ЗАЧИСТКУ';$('start').classList.remove('loading');$('start').onclick=()=>startGame(false)}catch(e){state.seed=Date.now()>>>0;$('start').classList.add('hidden');$('demo').classList.remove('hidden');$('demo').onclick=()=>startGame(true);$('startError').style.display='block';$('startError').textContent=e.message+' Доступен демонстрационный режим.'}}
async function finish(success,reason){if(state.finished)return;state.finished=true;state.running=false;cancelAnimationFrame(raf);fireHeld=false;const accuracy=state.shots?Math.min(100,Math.round(state.hits/state.shots*100)):0,raw=Math.max(0,Math.round(state.score+state.roomsCleared*55+(success?240:0)+player.hp*1.5));$('resultIcon').textContent=success?'☑':'☣';$('resultEyebrow').textContent=success?'КОМПЛЕКС ЗАЧИЩЕН':'ОПЕРАЦИЯ ПРОВАЛЕНА';$('resultTitle').textContent=success?'Зачистка завершена':'Отряд потерян';$('resultText').textContent=reason;$('resultScore').textContent=raw;$('resultKills').textContent=state.kills;$('resultRooms').textContent=state.roomsCleared+'/6';$('resultAccuracy').textContent=accuracy+'%';$('finish').classList.remove('hidden');if(state.demo||!state.sessionId){$('reward').textContent='Демо-режим: результат не начисляется.';return}try{const d=await api('finish',{session_id:state.sessionId,score:raw,stats:{cleared:success,kills:state.kills,rooms:state.roomsCleared,shots:state.shots,hits:state.hits,accuracy,hp:Math.round(player.hp),time_left:Math.round(state.time)}});$('reward').innerHTML=d.actual_reward>0?`Начислено <b>+${d.actual_reward}</b> влияния. Баланс: <b>${d.balance}</b>.`:(d.message||'Результат сохранён.')}catch(e){$('reward').textContent='Сервер не сохранил результат: '+e.message}}
function joyUpdate(e){const r=$('joystick').getBoundingClientRect(),cx=r.left+r.width/2,cy=r.top+r.height/2,dx=e.clientX-cx,dy=e.clientY-cy,max=r.width*.32,m=Math.hypot(dx,dy)||1,s=Math.min(1,max/m),x=dx*s,y=dy*s;joy.x=x/max;joy.y=y/max;$('joystickKnob').style.transform=`translate(calc(-50% + ${x}px),calc(-50% + ${y}px))`}
function joyReset(){joy.id=null;joy.x=joy.y=0;$('joystickKnob').style.transform='translate(-50%,-50%)'}
$('joystick').addEventListener('pointerdown',e=>{e.preventDefault();joy.id=e.pointerId;$('joystick').setPointerCapture?.(e.pointerId);joyUpdate(e)});$('joystick').addEventListener('pointermove',e=>{if(e.pointerId===joy.id)joyUpdate(e)});$('joystick').addEventListener('pointerup',joyReset);$('joystick').addEventListener('pointercancel',joyReset);
const fire=$('fire');fire.addEventListener('pointerdown',e=>{e.preventDefault();fireHeld=true;fire.classList.add('firing');fire.setPointerCapture?.(e.pointerId)});const stopFire=()=>{fireHeld=false;fire.classList.remove('firing')};fire.addEventListener('pointerup',stopFire);fire.addEventListener('pointercancel',stopFire);$('reload').onclick=reload;$('grenade').onclick=throwGrenade;$('back').onclick=goGames;$('toGames').onclick=goGames;$('again').onclick=()=>location.reload();window.addEventListener('keydown',e=>{keys.add(e.key.toLowerCase());if(e.code==='Space'){fireHeld=true;e.preventDefault()}if(e.key.toLowerCase()==='r')reload()});window.addEventListener('keyup',e=>{keys.delete(e.key.toLowerCase());if(e.code==='Space')fireHeld=false});document.addEventListener('visibilitychange',()=>{if(document.hidden){fireHeld=false;joyReset()}});new ResizeObserver(resize).observe(canvas);window.addEventListener('resize',resize);prepare();
})();
